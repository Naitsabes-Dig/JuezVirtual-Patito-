# Leer la línea de entrada, separarla por espacios y convertir cada elemento a un entero
numbers = list(map(int, input().split()))

# Encontrar y imprimir el número máximo de la lista
print(max(numbers))